# RHFiles 0.1.2

- 修复“修改日期”“创建日期”“类型”和“大小”排序只更新箭头、不改变文件顺序的问题。
- 主窗格与右窗格现在分别保存排序字段和升降序，排序后仍保持选中原文件。
- 默认对普通文件使用 Windows 默认打开程序提供的关联图标，`.rdc` 等文件会显示对应软件图标；文件夹继续使用 RHFiles 自定义图标。
- 同扩展名共用异步图标缓存，减少 Shell 查询，避免真实图标拖慢目录显示。
