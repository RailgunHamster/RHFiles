# RHFiles 0.1.7

- 自动更新会在主窗口启动后、打开设置时以及软件持续运行期间每小时检查一次；关闭“自动检查更新”后，仅手动点击“立即检查”才会联网。
- 修复 GitHub 更新检查不读取应用内代理、重复请求多个历史 Release 且可能触发 API 限额的问题，改为读取最新 Release 的累计 Velopack 索引并增加请求超时。
- 设置新增 HTTP/HTTPS 网络代理开关和代理地址，可用于 GitHub 更新检查及更新包下载；支持 `127.0.0.1:7890` 和完整 URL 格式。
