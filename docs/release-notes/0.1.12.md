# RHFiles 0.1.12

- 新增 `Shift+Delete` 永久删除：操作会绕过回收站，并连续进行两次确认后才会执行。
- 永久删除也支持多选、右键菜单和可配置快捷键；该操作不会错误地加入可撤销历史。
- 支持在不同 RHFiles 窗口之间拖放多个文件或文件夹，松开后可明确选择复制、移动或取消。
- 跨窗口拖放沿用重名冲突处理与可撤销操作记录；移动完成后会同步刷新源窗口和目标窗口。
- 详情、卡片、缩略图和分栏视图均可发起文件拖放，并可直接将项目放入目标文件夹。
