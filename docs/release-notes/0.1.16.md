# RHFiles 0.1.16

- 标签页右键菜单新增“固定标签页/取消固定标签页”；固定状态同时支持左、右窗格，并在重启后保留。
- 固定标签显示清晰的图钉标记、始终排列在普通标签之前，并隐藏容易误点的关闭按钮。
- “关闭其他标签页”和“关闭右侧标签页”会保护固定标签；拖动排序可在固定区内自由调整，但不会跨越固定区边界。
- 当前文件夹空白处的右键菜单新增“打开方式”子菜单，可直接用 VS Code、Visual Studio、CMD、PowerShell、Git Bash 等工具打开当前目录。
